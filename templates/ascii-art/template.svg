<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <defs>
    <linearGradient id="bg-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.bg_color | default('#1e1e2e') }}"/>
      <stop offset="100%" stop-color="{{ brand.bg_color_end | default('#11111b') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad-h" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <radialGradient id="glow" cx="75%" cy="25%" r="55%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0.07"/>
      <stop offset="100%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0"/>
    </radialGradient>
    <pattern id="dots" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
      <circle cx="20" cy="20" r="1.5" fill="#ffffff" opacity="0.06"/>
    </pattern>
  </defs>
{% if bg_image_data_uri %}
  <image href="{{ bg_image_data_uri }}" width="1200" height="630" preserveAspectRatio="xMidYMid slice" opacity="{{ brand.bg_image_opacity | default(0.15) }}"/>
  {% endif %}
  <rect width="1200" height="630" fill="url(#bg-grad)" opacity="{% if bg_image_data_uri %}0.7{% else %}1.0{% endif %}"/>
  <rect width="1200" height="630" fill="url(#glow)"/>
  <rect width="1200" height="630" fill="url(#dots)"/>
  <text x="600" y="50" text-anchor="middle" font-family="Space Grotesk,sans-serif" font-size="22" font-weight="700" fill="#cdd6f4">{{ slide.title }}</text>
  <rect x="80" y="75" width="1040" height="490" rx="12" fill="#0c0c14" opacity="0.85"/>
  <text x="600" y="160" text-anchor="middle" font-family="JetBrains Mono,monospace" font-size="22" fill="url(#acc-grad)">{{ slide.line1 }}</text>
  <text x="600" y="210" text-anchor="middle" font-family="JetBrains Mono,monospace" font-size="22" fill="url(#acc-grad)">{{ slide.line2 }}</text>
  <text x="600" y="260" text-anchor="middle" font-family="JetBrains Mono,monospace" font-size="22" fill="url(#acc-grad)">{{ slide.line3 }}</text>
  <text x="600" y="310" text-anchor="middle" font-family="JetBrains Mono,monospace" font-size="22" fill="url(#acc-grad)">{{ slide.line4 }}</text>
  <text x="600" y="360" text-anchor="middle" font-family="JetBrains Mono,monospace" font-size="22" fill="url(#acc-grad)">{{ slide.line5 }}</text>
  <text x="600" y="410" text-anchor="middle" font-family="JetBrains Mono,monospace" font-size="22" fill="url(#acc-grad)">{{ slide.line6 }}</text>
  {% if slide.caption %}
  <line x1="300" y1="470" x2="900" y2="470" stroke="#313244" stroke-width="1" opacity="0.2"/>
  <text x="600" y="510" text-anchor="middle" font-family="Inter,sans-serif" font-size="18" fill="#7f849c">{{ slide.caption }}</text>
  {% endif %}
  {% if brand.show_brand %}
  <text x="600" y="615" text-anchor="middle" font-family="Inter,sans-serif" font-size="20" font-weight="600" fill="#7f849c">{{ brand.brand_name | default('') }}</text>
  {% endif %}
</svg>