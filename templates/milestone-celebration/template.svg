<svg xmlns="http://www.w3.org/2000/svg" width="1080" height="1080" viewBox="0 0 1080 1080">
  <defs>
    <linearGradient id="bg-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.bg_color | default('#1e1e2e') }}"/>
      <stop offset="100%" stop-color="{{ brand.bg_color_end | default('#11111b') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad-h" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <radialGradient id="glow" cx="75%" cy="25%" r="55%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0.07"/>
      <stop offset="100%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0"/>
    </radialGradient>
    <pattern id="dots" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
      <circle cx="20" cy="20" r="1.5" fill="#ffffff" opacity="0.06"/>
    </pattern>
  </defs>
{% if bg_image_data_uri %}
  <image href="{{ bg_image_data_uri }}" width="1080" height="1080" preserveAspectRatio="xMidYMid slice" opacity="{{ brand.bg_image_opacity | default(0.15) }}"/>
  {% endif %}
  <rect width="1080" height="1080" fill="url(#bg-grad)" opacity="{% if bg_image_data_uri %}0.7{% else %}1.0{% endif %}"/>
  <rect width="1080" height="1080" fill="url(#glow)"/>
  <rect width="1080" height="1080" fill="url(#dots)"/>
  <circle cx="200" cy="200" r="6" fill="#f9e2af" opacity="0.5"/>
  <circle cx="850" cy="150" r="8" fill="#a6e3a1" opacity="0.4"/>
  <circle cx="920" cy="300" r="5" fill="#cba6f7" opacity="0.5"/>
  <circle cx="150" cy="400" r="7" fill="#89b4fa" opacity="0.4"/>
  <circle cx="880" cy="500" r="6" fill="#f9e2af" opacity="0.3"/>
  <circle cx="120" cy="600" r="5" fill="#a6e3a1" opacity="0.4"/>
  <text x="540" y="450" text-anchor="middle" font-family="Space Grotesk,sans-serif" font-size="160" font-weight="900" fill="url(#acc-grad)">{{ slide.milestone }}<tspan font-size="72" fill="#cdd6f4" opacity="0.6">{% if slide.milestone_suffix %}{{ slide.milestone_suffix }}{% endif %}</tspan></text>
  <rect x="440" y="510" width="200" height="3" rx="1.5" fill="url(#acc-grad-h)" opacity="0.4"/>
  <text x="540" y="580" text-anchor="middle" font-family="Space Grotesk,sans-serif" font-size="40" font-weight="700" fill="#cdd6f4">
{% for line in title_lines %}    <tspan x="540" dy="{% if loop.first %}0{% else %}50{% endif %}">{{ line }}</tspan>{% endfor %}
    {% if not title_lines %}<tspan x="540">{{ slide.title }}</tspan>{% endif %}
  </text>
  {% if slide.message %}
  <text x="540" y="740" text-anchor="middle" font-family="Inter,sans-serif" font-size="24" font-weight="400" fill="#a6adc8">
{% for line in message_lines %}    <tspan x="540" dy="{% if loop.first %}0{% else %}36{% endif %}">{{ line }}</tspan>{% endfor %}
    {% if not message_lines %}<tspan x="540">{{ slide.message }}</tspan>{% endif %}
  </text>
  {% endif %}
  {% if brand.show_brand %}
  <text x="540" y="1050" text-anchor="middle" font-family="Inter,sans-serif" font-size="20" font-weight="600" fill="#7f849c">{{ brand.brand_name | default('') }}</text>
  {% endif %}
</svg>