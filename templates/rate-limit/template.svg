<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <defs>
    <linearGradient id="bg-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.bg_color | default('#1e1e2e') }}"/>
      <stop offset="100%" stop-color="{{ brand.bg_color_end | default('#11111b') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad-h" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <radialGradient id="glow" cx="75%" cy="25%" r="55%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0.07"/>
      <stop offset="100%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0"/>
    </radialGradient>
    <pattern id="dots" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
      <circle cx="20" cy="20" r="1.5" fill="#ffffff" opacity="0.06"/>
    </pattern>
  </defs>
{% if bg_image_data_uri %}
  <image href="{{ bg_image_data_uri }}" width="1200" height="630" preserveAspectRatio="xMidYMid slice" opacity="{{ brand.bg_image_opacity | default(0.15) }}"/>
  {% endif %}
  <rect width="1200" height="630" fill="url(#bg-grad)" opacity="{% if bg_image_data_uri %}0.7{% else %}1.0{% endif %}"/>
  <rect width="1200" height="630" fill="url(#glow)"/>
  <rect width="1200" height="630" fill="url(#dots)"/>
  <text x="600" y="55" text-anchor="middle" font-family="Space Grotesk,sans-serif" font-size="22" font-weight="700" fill="#cdd6f4">{{ slide.title }}</text>
  <text x="600" y="180" text-anchor="middle" font-family="Space Grotesk,sans-serif" font-size="90" font-weight="800" fill="url(#acc-grad)">{{ slide.remaining }}</text>
  <text x="600" y="230" text-anchor="middle" font-family="Inter,sans-serif" font-size="24" fill="#7f849c">of {{ slide.limit }} requests remaining</text>
  {% set pct = (slide.used_pct | float) %}
  {% set is_critical = pct > 80 %}
  {% set is_warning = pct > 50 and pct <= 80 %}
  <rect x="100" y="290" width="1000" height="24" rx="12" fill="#313244" opacity="0.2"/>
  <rect x="100" y="290" width="{{ pct * 10 }}" height="24" rx="12" fill="{% if is_critical %}#f38ba8{% elif is_warning %}#f9e2af{% else %}#a6e3a1{% endif %}" opacity="0.6"/>
  <text x="100" y="360" font-family="Inter,sans-serif" font-size="18" fill="#7f849c">{{ slide.used_pct }}% used</text>
  <text x="1100" y="360" text-anchor="end" font-family="Inter,sans-serif" font-size="18" fill="#7f849c">Resets in {{ slide.reset }}</text>
  {% if slide.plan %}
  <rect x="450" y="420" width="300" height="60" rx="30" fill="url(#acc-grad)" opacity="0.12"/>
  <text x="600" y="460" text-anchor="middle" font-family="Space Grotesk,sans-serif" font-size="22" font-weight="700" fill="url(#acc-grad)">{{ slide.plan }} Plan</text>
  {% endif %}
  {% if brand.show_brand %}
  <text x="600" y="615" text-anchor="middle" font-family="Inter,sans-serif" font-size="20" font-weight="600" fill="#7f849c">{{ brand.brand_name | default('') }}</text>
  {% endif %}
</svg>