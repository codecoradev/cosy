{
  "id": "social-quote",
  "name": "Social Quote",
  "dimensions": {
    "width": 1080,
    "height": 1350
  },
  "fonts": [
    "Inter"
  ],
  "brand_fields": {
    "show_brand": {
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Show brand name/handle watermark at bottom. Default: false."
    },
    "brand_name": {
      "type": "text",
      "required": true,
      "max": 30
    },
    "brand_handle": {
      "type": "text",
      "required": true,
      "max": 30
    },
    "bg_color": {
      "type": "bg",
      "required": false,
      "default": "#1e1e2e"
    },
    "bg_color_end": {
      "type": "bg",
      "required": false,
      "default": "#11111b"
    },
    "accent_color": {
      "type": "text",
      "required": false,
      "default": "#cba6f7"
    },
    "accent_color_end": {
      "type": "text",
      "required": false,
      "default": "#89b4fa"
    },
    "bg_image": {
      "type": "image",
      "required": false,
      "description": "Background image path (PNG/JPG/WebP). Rendered with configurable opacity behind gradient overlay."
    },
    "bg_image_opacity": {
      "type": "number",
      "required": false,
      "default": 0.15,
      "description": "Opacity of background image (0.0-1.0). Lower = more subtle."
    },
    "logo": {
      "type": "image",
      "required": false,
      "description": "Brand logo path (PNG/SVG). Rendered at bottom-right corner."
    },
    "bg_overlay_opacity": {
      "type": "number",
      "required": false,
      "default": 0.7,
      "description": "Opacity of the gradient overlay when bg_image is set. 0.0 = photo fully visible, 1.0 = gradient fully covers. Default: 0.7."
    }
  },
  "slide_fields": {
    "quote": {
      "type": "text",
      "required": true,
      "max": 280,
      "wrap_width": 35,
      "options": ["markup"],
      "description": "Quote text. Supports inline markup: *bold*, _italic_, *color:#hex*text*color*."
    },
    "author": {
      "type": "text",
      "required": true,
      "max": 50
    },
    "author_title": {
      "type": "text",
      "required": false,
      "max": 50
    },
    "text_color": {
      "type": "color",
      "required": false,
      "description": "Text color for quote/title text as hex (e.g. \"#1e1e2e\"). Use when the background is light. Default: template dark theme color."
    },
    "hashtag": {
      "type": "text",
      "required": false,
      "max": 40,
      "description": "Hashtag shown near the bottom, rendered with a leading # (pass without the # symbol)."
    }
  }
}
