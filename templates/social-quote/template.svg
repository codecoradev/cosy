<svg xmlns="http://www.w3.org/2000/svg" width="1080" height="1350" viewBox="0 0 1080 1350">
  <defs>
    <linearGradient id="bg-gradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.bg_color | default('#1e1e2e') }}"/>
      <stop offset="100%" stop-color="{{ brand.bg_color_end | default('#11111b') }}"/>
    </linearGradient>
    <linearGradient id="accent-gradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <radialGradient id="glow" cx="70%" cy="30%" r="55%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0.06"/>
      <stop offset="100%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0"/>
    </radialGradient>
  <pattern id="dots" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
    <circle cx="20" cy="20" r="1.5" fill="#ffffff" opacity="0.06"/>
  </pattern>
  </defs>

  <!-- Gradient background + glow -->
  {% if bg_image_data_uri %}
  <image href="{{ bg_image_data_uri }}" width="1080" height="1350" preserveAspectRatio="xMidYMid slice" opacity="{{ brand.bg_image_opacity | default(0.15) }}"/>
  {% endif %}
  <rect width="1080" height="1350" fill="url(#bg-gradient)" opacity="{% if bg_image_data_uri %}0.7{% else %}1.0{% endif %}"/>
  <rect width="1080" height="1350" fill="url(#glow)"/>
  <rect width="1080" height="1350" fill="url(#dots)"/>

  <!-- Accent bar -->
  <rect x="80" y="120" width="8" height="80" rx="8" fill="url(#accent-gradient)"/>

  <!-- Quote text (wrapped) -->
  <text x="80" y="420" font-family="Inter, sans-serif" font-size="52" font-weight="700" fill="#cdd6f4">
    {% for line in quote_lines %}
    <tspan x="80" dy="{% if loop.first %}0{% else %}68{% endif %}">{{ line }}</tspan>
    {% endfor %}
    {% if not quote_lines %}
    <tspan x="80" dy="0">{{ slide.quote }}</tspan>
    {% endif %}
  </text>

  <!-- Author -->
  {% if slide.author %}
  <text x="80" y="1000" font-family="Inter, sans-serif" font-size="32" font-weight="500" fill="{{ brand.accent_color | default('#cba6f7') }}">
    — {{ slide.author }}
  </text>
  {% endif %}

  <!-- Brand footer -->
  {% if brand.show_brand %}
  <text x="540" y="1320" text-anchor="middle" font-family="Inter,sans-serif" font-size="20" font-weight="600" fill="#7f849c">
    {{ brand.brand_name | default('') }} · {{ brand.brand_handle | default('') }}
  </text>
  {% endif %}
  <circle cx="1000" cy="80" r="180" fill="{{ brand.accent_color | default('#cba6f7') }}" opacity="0.04"/>
  {% if logo_data_uri %}
  <image href="{{ logo_data_uri }}" x="952" y="1222" height="48" preserveAspectRatio="xMidYMid meet"/>
  {% endif %}
</svg>
