<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <defs>
    <linearGradient id="bg-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.bg_color | default('#1e1e2e') }}"/>
      <stop offset="100%" stop-color="{{ brand.bg_color_end | default('#11111b') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad-h" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <radialGradient id="glow" cx="75%" cy="25%" r="55%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0.07"/>
      <stop offset="100%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0"/>
    </radialGradient>
    <pattern id="dots" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
      <circle cx="20" cy="20" r="1.5" fill="#ffffff" opacity="0.06"/>
    </pattern>
  </defs>
{% if bg_image_data_uri %}
  <image href="{{ bg_image_data_uri }}" width="1200" height="630" preserveAspectRatio="xMidYMid slice" opacity="{{ brand.bg_image_opacity | default(0.15) }}"/>
  {% endif %}
  <rect width="1200" height="630" fill="url(#bg-grad)" opacity="{% if bg_image_data_uri %}0.7{% else %}1.0{% endif %}"/>
  <rect width="1200" height="630" fill="url(#glow)"/>
  <rect width="1200" height="630" fill="url(#dots)"/>
  <text x="600" y="280" text-anchor="middle" font-family="Space Grotesk,sans-serif" font-size="120" font-weight="900" fill="url(#acc-grad)">{{ slide.stat_value }}{% if slide.stat_suffix %}<tspan font-size="56" fill="#cdd6f4">{{ slide.stat_suffix }}</tspan>{% endif %}</text>
  <text x="600" y="360" text-anchor="middle" font-family="Inter,sans-serif" font-size="30" font-weight="600" fill="#cdd6f4">
{% for line in label_lines %}    <tspan x="600" dy="{% if loop.first %}0{% else %}40{% endif %}">{{ line }}</tspan>{% endfor %}
    {% if not label_lines %}<tspan x="600">{{ slide.label }}</tspan>{% endif %}
  </text>
  {% if slide.context %}
  <text x="600" y="490" text-anchor="middle" font-family="Inter,sans-serif" font-size="22" font-weight="400" fill="#7f849c">
{% for line in context_lines %}    <tspan x="600" dy="{% if loop.first %}0{% else %}30{% endif %}">{{ line }}</tspan>{% endfor %}
    {% if not context_lines %}<tspan x="600">{{ slide.context }}</tspan>{% endif %}
  </text>
  {% endif %}
  {% if brand.show_brand %}
  <text x="600" y="595" text-anchor="middle" font-family="Inter,sans-serif" font-size="20" font-weight="600" fill="#7f849c">{{ brand.brand_name | default('') }}</text>
  {% endif %}
</svg>