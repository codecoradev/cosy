<svg xmlns="http://www.w3.org/2000/svg" width="1080" height="1080" viewBox="0 0 1080 1080">
  <defs>
    <linearGradient id="bg-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.bg_color | default('#1e1e2e') }}"/>
      <stop offset="100%" stop-color="{{ brand.bg_color_end | default('#11111b') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad-h" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <radialGradient id="glow" cx="75%" cy="25%" r="55%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0.07"/>
      <stop offset="100%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0"/>
    </radialGradient>
    <pattern id="dots" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
      <circle cx="20" cy="20" r="1.5" fill="#ffffff" opacity="0.06"/>
    </pattern>
  </defs>
{% if bg_image_data_uri %}
  <image href="{{ bg_image_data_uri }}" width="1080" height="1080" preserveAspectRatio="xMidYMid slice" opacity="{{ brand.bg_image_opacity | default(0.15) }}"/>
  {% endif %}
  <rect width="1080" height="1080" fill="url(#bg-grad)" opacity="{% if bg_image_data_uri %}0.7{% else %}1.0{% endif %}"/>
  <rect width="1080" height="1080" fill="url(#glow)"/>
  <rect width="1080" height="1080" fill="url(#dots)"/>
  {% if slide.title %}
  <text x="540" y="100" text-anchor="middle" font-family="Space Grotesk,sans-serif" font-size="36" font-weight="700" fill="#cdd6f4">{{ slide.title }}</text>
  {% endif %}
  {% set testimonials = [
    (slide.t1_text, slide.t1_author, t1_text_lines),
    (slide.t2_text, slide.t2_author, t2_text_lines),
    (slide.t3_text, slide.t3_author, t3_text_lines)
  ] %}
  {% set positions = [180, 540, 900] %}
  {% for t in testimonials %}
  {% if t[0] %}
  {% set cx = positions[loop.index0] %}
  <rect x="{{ cx - 150 }}" y="200" width="300" height="680" rx="20" fill="#313244" opacity="0.35"/>
  <text x="{{ cx | string + ' ' }}" y="0" opacity="0" fill="none"></text>
  <text x="{{ cx }}" y="260" text-anchor="middle" font-family="Space Grotesk,sans-serif" font-size="48" font-weight="900" fill="url(#acc-grad)" opacity="0.2">&amp;</text>
  {% set tl = t[2] %}
  {% if tl %}
    {% for line in tl %}
  <text x="{{ cx }}" y="{{ 340 + loop.index0 * 34 }}" text-anchor="middle" font-family="Inter,sans-serif" font-size="20" font-weight="400" fill="#cdd6f4">{{ line }}</text>
    {% endfor %}
  {% else %}
  <text x="{{ cx }}" y="340" text-anchor="middle" font-family="Inter,sans-serif" font-size="20" font-weight="400" fill="#cdd6f4">{{ t[0] }}</text>
  {% endif %}
  <rect x="{{ cx - 40 }}" y="760" width="80" height="2" rx="8" fill="url(#acc-grad-h)"/>
  <text x="{{ cx }}" y="810" text-anchor="middle" font-family="Inter,sans-serif" font-size="18" font-weight="600" fill="url(#acc-grad)">{{ t[1] }}</text>
  {% endif %}
  {% endfor %}
  {% if brand.show_brand %}
  <text x="540" y="1050" text-anchor="middle" font-family="Inter,sans-serif" font-size="20" font-weight="600" fill="#7f849c">{{ brand.brand_name | default('') }}</text>
  {% endif %}
</svg>