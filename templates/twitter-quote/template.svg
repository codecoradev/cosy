<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="675" viewBox="0 0 1200 675">
  <defs>
    <linearGradient id="bg-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.bg_color | default('#1e1e2e') }}"/>
      <stop offset="100%" stop-color="{{ brand.bg_color_end | default('#11111b') }}"/>
    </linearGradient>
    <linearGradient id="acc-grad" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}"/>
      <stop offset="100%" stop-color="{{ brand.accent_color_end | default('#89b4fa') }}"/>
    </linearGradient>
    <radialGradient id="glow" cx="75%" cy="25%" r="55%">
      <stop offset="0%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0.07"/>
      <stop offset="100%" stop-color="{{ brand.accent_color | default('#cba6f7') }}" stop-opacity="0"/>
    </radialGradient>
  </defs>
  <rect width="1200" height="675" fill="url(#bg-grad)"/>
  <rect width="1200" height="675" fill="url(#glow)"/>
  <rect x="80" y="80" width="6" height="80" rx="3" fill="url(#acc-grad)"/>
  <text x="110" y="120" font-family="Inter,sans-serif" font-size="28" font-weight="600" fill="{{ brand.accent_color | default('#cba6f7') }}">{{ brand.brand_name | default('') }}</text>
  <text x="80" y="280" font-family="Inter,sans-serif" font-size="42" font-weight="700" fill="#cdd6f4">
    {% for line in quote_lines %}<tspan x="80" dy="{% if loop.first %}0{% else %}55{% endif %}">{{ line }}</tspan>{% endfor %}
    {% if not quote_lines %}<tspan x="80" dy="0">{{ slide.quote }}</tspan>{% endif %}
  </text>
  <text x="80" y="560" font-family="Inter,sans-serif" font-size="26" font-weight="600" fill="#bac2de">{{ slide.author }}</text>
  <text x="80" y="595" font-family="Inter,sans-serif" font-size="22" font-weight="400" fill="#7f849c">{{ slide.handle | default(brand.brand_handle) }}</text>
</svg>
